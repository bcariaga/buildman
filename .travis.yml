language: node_js
sudo: false
node_js:
  - "8.11"
install:
  - npm install
  - npm install -g codecov
script:
  - npm test
  - nyc report --reporter=text-lcov > coverage.lcov
  - ./node_modules/.bin/codecov
